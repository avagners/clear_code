1. 
```python
// Связывание с переменной во время написания кода. В префиксе обозначается тип документа. Он изменяться не должен. Поэтому переменную связываем сразу.

...
if values[0][-2] == 'Отгрузка':
    prefix = 'INVOICE_'
...
```

2. 
```python
// Связывание переменной выполняется во время компиляции кода. Нужна возможность корректировки возраста писем, старше которого нужно удалять из почтового ящика. Поэтому кол-во дней вынесено в константу для параметризации.

DAYS = 90
...
delete_old_emails(imap_conn_name=connection, days=DAYS)
```

3. 
```python
// Cвязывание переменной во время выполнения программы. В скрипте нужна дата запуска.

date_today = date.today()
...
```